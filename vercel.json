{
  "buildCommand": "cd task-manager-frontend && npm install && npm run build",
  "outputDirectory": "task-manager-frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}

